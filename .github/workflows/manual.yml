name: TheGreatDataDump

on:
  schedule:
    - cron: '0 7 * * *'

  workflow_dispatch:

jobs:
  run:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Python script
        run: python TheGreatDataDump.py
